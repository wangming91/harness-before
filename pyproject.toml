[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "abh"
version = "0.1.0"
description = "Attractor Before Harness CLI"
requires-python = ">=3.13"

[project.scripts]
abh = "abh.cli:main"

[tool.setuptools]
packages = ["abh"]
